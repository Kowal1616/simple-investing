services:
  investing-app:
    build: .
    ports:
      - "80:5000"
    env_file:
      - .env
    volumes:
      # Only mount the instance folder for the SQLite database
      - ./instance:/app/instance
    restart: unless-stopped
