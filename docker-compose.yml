services:
  investing-app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      # Only mount the instance folder for the SQLite database
      - ./instance:/app/instance
    restart: unless-stopped
